name: material_toolkit_example
description: ""

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  material_design: ^0.2.5
  # material_design:
  #   path: ../packages/material_design
  material_toolkit:
    path: ../
  provider: ^6.1.5

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
